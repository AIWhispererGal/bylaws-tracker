<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome - Bylaws Amendment Tracker</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .landing-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      padding: 4rem 3rem;
      max-width: 600px;
      width: 100%;
      margin: 2rem;
      text-align: center;
    }

    .logo {
      font-size: 4rem;
      color: #667eea;
      margin-bottom: 1rem;
    }

    h1 {
      color: #333;
      font-weight: bold;
      margin-bottom: 1rem;
      font-size: 2.5rem;
    }

    .tagline {
      color: #6c757d;
      font-size: 1.2rem;
      margin-bottom: 3rem;
      line-height: 1.6;
    }

    .action-buttons {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .btn-custom {
      padding: 1rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 12px;
      border: none;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }

    .btn-primary-custom {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
    }

    .btn-primary-custom:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
    }

    .btn-outline-custom {
      background: white;
      color: #667eea;
      border: 2px solid #667eea;
    }

    .btn-outline-custom:hover {
      background: #667eea;
      color: white;
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.2);
    }

    .divider {
      display: flex;
      align-items: center;
      text-align: center;
      margin: 2rem 0;
      color: #6c757d;
    }

    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      border-bottom: 1px solid #dee2e6;
    }

    .divider span {
      padding: 0 1rem;
      font-size: 0.9rem;
    }

    .features {
      text-align: left;
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 1px solid #dee2e6;
    }

    .feature-item {
      display: flex;
      align-items: start;
      margin-bottom: 1rem;
      color: #495057;
    }

    .feature-item i {
      color: #667eea;
      margin-right: 0.75rem;
      margin-top: 0.25rem;
      font-size: 1.2rem;
    }

    .feature-item span {
      flex: 1;
    }

    @media (max-width: 576px) {
      .landing-container {
        padding: 2rem 1.5rem;
      }

      h1 {
        font-size: 2rem;
      }

      .tagline {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="landing-container">
    <div class="logo">
      <i class="bi bi-file-text"></i>
    </div>

    <h1>Bylaws Amendment Tracker</h1>

    <p class="tagline">
      Streamline your organization's amendment process with collaborative tools and transparent workflows.
    </p>

    <div class="action-buttons">
      <a href="/auth/login" class="btn btn-custom btn-primary-custom">
        <i class="bi bi-box-arrow-in-right"></i>
        Login
      </a>

      <a href="/auth/register" class="btn btn-custom btn-outline-custom">
        <i class="bi bi-person-plus"></i>
        Create Account
      </a>
    </div>

    <div class="divider">
      <span>OR</span>
    </div>

    <a href="/setup" class="btn btn-custom btn-outline-custom">
      <i class="bi bi-gear"></i>
      Setup New Organization
    </a>

    <div class="features">
      <div class="feature-item">
        <i class="bi bi-people"></i>
        <span>Multi-tenant platform supporting multiple organizations</span>
      </div>
      <div class="feature-item">
        <i class="bi bi-shield-check"></i>
        <span>Row-level security ensuring complete data isolation</span>
      </div>
      <div class="feature-item">
        <i class="bi bi-diagram-3"></i>
        <span>Customizable approval workflows for any governance structure</span>
      </div>
      <div class="feature-item">
        <i class="bi bi-chat-square-text"></i>
        <span>Collaborative amendment suggestions and voting</span>
      </div>
    </div>
  </div>

  <script>
    // Universal Supabase auth callback handler
    // Automatically redirects password reset tokens to the correct page
    (function() {
      // Check if there's a URL fragment (hash)
      const hash = window.location.hash;

      if (hash && hash.length > 1) {
        // Parse the hash parameters
        const params = new URLSearchParams(hash.substring(1));
        const type = params.get('type');
        const accessToken = params.get('access_token');

        // If this is a password recovery token, redirect to reset password page
        if (type === 'recovery' && accessToken) {
          console.log('Password reset token detected, redirecting to reset password page...');
          window.location.href = '/auth/reset-password' + hash;
          return;
        }

        // If this is an email verification token, could handle it here too
        if (type === 'signup' && accessToken) {
          console.log('Email verification token detected');
          // Could redirect to a verification success page
        }
      }
    })();
  </script>
</body>
</html>
